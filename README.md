# 深度学习课程学期总结报告

## 第一章     课程学习总结

### 1.1 所学主要知识点概述

| 模型名称 | 模型应用场景 | 模型优点 | 模型缺点 |
| --------- | -------------- | --------- |---------- |
| Linear Regression  | 房价预测、股票趋势分析、销售额预估等简单数值预测场景 | 原理简单易懂，计算效率高 | 仅能处理线性关系，无法捕捉复杂非线性模式 |
| Logistic Regression | 垃圾邮件分类、疾病检测、用户是否点击广告等二分类任务 | 模型简单高效，能输出分类概率 | 仅适用于二分类问题，对非线性关系处理能力有限 |
| MLP | 图像简单分类（如 MNIST 手写数字识别）、文本基础分类、简单模式识别 | 可处理非线性问题，理论上能拟合任意复杂函数，结构灵活易实现 | 全连接层参数量大，容易过拟合，对数据预处理要求较高，训练耗时 |
| CNN | 图像分类（如 ImageNet）、目标检测（如 YOLO）、语义分割、视频分析 | 能自动提取空间特征，卷积操作可减少参数数量，对平移、旋转等变换具有不变性 | 对序列数据（如长文本）处理能力较弱，难以捕捉全局依赖关系，计算复杂度较高 |
| RNN | 文本生成（如诗词创作）、语音识别、时间序列预测（如股票走势） | 适合处理序列数据，能捕捉序列中的时序依赖关系，结构设计符合序列处理逻辑 | 存在长期依赖问题，梯度容易消失或爆炸，无法并行处理长序列，训练效率低 |
| LSTM | 机器翻译、情感分析、视频字幕生成、复杂时间序列预测 | 通过门控机制解决 RNN 长期依赖问题，能有效捕捉长距离时序依赖 | 结构复杂，参数数量多，训练耗时较长，计算成本高于简单 RNN |
| GRU | 文本摘要生成、语音情感识别、短文本序列处理| 作为 LSTM 简化版，参数更少，训练速度更快，计算效率高，在多数序列任务中性能接近 LSTM | 捕捉长期依赖的能力略弱于 LSTM，门控机制相对简单 |
| Transformer | 机器翻译（如 Google Translate）、文本生成（如 GPT 系列）、问答系统、多模态任务 | 完全基于注意力机制，可并行计算，能高效捕捉全局依赖关系，推动 NLP 进入预训练时代 | 对长序列计算复杂度高（平方级），缺乏时序归纳偏置，初期训练需要大量数据 |
| GANS | 图像生成（如人脸、风景）、风格迁移（如照片转油画）、数据增强、虚拟人物创建 | 生成样本质量高，可捕捉数据分布的潜在规律，无需显式定义损失函数 | 训练过程不稳定（模式崩溃问题），生成结果不可控，评估指标不完善 |
| VAE | 图像压缩、无监督特征学习、生成式建模（如人脸插值）、数据降维 | 基于概率模型，生成样本具有连续性，可解释性较强，适合无监督学习场景 | 生成样本清晰度可能低于 GAN，优化目标包含 KL 散度约束，存在模糊化倾向 |

### 1.2 重要概念

***1.注意力机制：***

- **概念**：注意力机制就是通过计算“查询（Query）”与“键（Key）”之间的相关性，为“值（Value）”分配权重，从而实现对输入序列的动态聚焦。
- **直观理解**：注意力机制模拟了人类观察事物时的“聚焦能力”，让模型关注输入序列中的关键部分。通过Q和K之间的关系，给V分配权重。
- **数学本质**：加权求和，将输入序列Value根据重要性权重重新组合，权重由Query和Key的相似度计算得到。

***2.自动编码器：***

- **概念**：Autoencoder是一种无监督学习模型，核心目标是通过编码（Encoder）和解码（Decoder）过程，实现对输入数据的“无损重建”
- **作用**：Autoencoder可以将高维（如图像，文本）数据压缩为低维隐向量，自动学习数据的本质特征。并且可以重建已知数据，具备生成新样本的能力





***3.正则化：***

- **Dorpout**：在神经网络训练过程中，以一定概率（如p=0.5）随机将神经元输出置为0，使模型无法过度依赖某些特定神经元，强制学习更加鲁棒的特征。
- **Batch Normalization**：对每个批次的输入数据进行标准化处理，使数据分布维持在均值为0，方差为1的范围，缓解“内部协变量偏移”。对每个特征维度计算这个批次的均值和方差。
- **Layer Normalization**：对单个样本的所有特征维度进行标准化，适合序列长度可变的任务（NLP），对单个样本的隐藏层输入计算均值和方差。
- **Data Augmentation**：通过对原始数据进行变换（如旋转、缩放、裁剪等）生成新样本，扩充数据集规模，使模型学习更具泛化性的特征。

### 1.3 掌握程度自评

| 知识点     | 掌握程度(1-5分)    |  备注  |
| --------   | ------------------   | ----  |
| Linear Regression | 4 | 简单 |
| Logistic Regression | 4 | 简单 |
| MLP | 4 | 比较简单 |
| CNN | 3 | 还行 |
| RNN | 1 | 太难，不理解文本 |
| LSTM | 1 | 太难，门控太多 |
| GRU | 1 | 太难 |
| Transformer | 2 | 太难 |
| GANS | 3 | 还行 |
| VAE | 3 | 还行 |

## 第二章     实践项目总结

### 2.1 猪肉价格预测问题

**2.1.1 项目概述**：

- **项目名称**：猪肉价格预测
- **项目背景与目标**：猪肉作为我国居民主要的肉类消费品，其价格波动不仅影响居民日常生活成本，还对农业经济、物价指数等产生重要影响。本项目基于过去 2 - 5 月份的猪肉价格变化数据，旨在运用深度学习技术，构建精准的猪肉价格预测模型，预测未来 6 月份的猪肉价格，为养殖户、经销商、政策制定者等提供决策参考依据，助力稳定猪肉市场供需平衡。
- **使用的技术栈**：Python 作为核心开发语言，具备丰富的第三方库支持；PyTorch 用于搭建深度学习模型，实现模型的训练与优化；BeautifulSoup 则用于从网页中提取猪肉价格相关数据，获取项目所需原始数据集。
- **项目实践周期**：one week

**2.1.2 项目概述**：

- **数据准备与预处理方法**：使用 Python 编写爬虫程序，结合 BeautifulSoup 库，从权威的农业数据网站、行业资讯平台等，爬取 2 月到 5 月三个月的猪肉价格数据集。获取数据后，首先对数据进行清洗，去除重复记录、无效数据；针对缺失值，采用均值填充、线性插值等方法进行补充；对于异常值，通过统计分析识别并修正，确保数据的完整性和准确性。最后将处理后的数据进行归一化处理，以便于模型更好地学习数据特征。
- **模型设计思路与架构图**：考虑到猪肉价格数据具有时间序列特性且可能存在局部特征，采用两层卷积层提取数据的局部特征，捕捉价格变化的短期规律；叠加 3 层 LSTM 层和 4 层 GRU 层，利用它们在处理长序列数据上的优势，学习价格数据的长期依赖关系；最后通过一层注意力层，让模型能够聚焦于对价格预测更重要的时间步，提升预测精度。
- **训练过程与调参经验**：模型训练过程中，设置训练 100 个 epoch，采用随机梯度下降（SGD）作为优化器，初始学习率设为 0.001，每 10 个 epoch 进行一次学习率衰减，以平衡模型的收敛速度和精度。在训练过程中，密切关注损失函数的变化，若损失值在多个 epoch 内不再明显下降，则适当调整批次大小或优化器参数。通过多次实验对比，最终确定了较优的超参数组合，使模型在训练集和验证集上都取得较好的性能。
- **结果评估指标**：采用准确率作为主要评估指标，通过计算预测价格与实际价格的误差，将误差在一定范围内的数据视为预测准确，统计准确数据占总数据的比例得到准确率。同时，结合均方误差（MSE）、平均绝对误差（MAE）等指标，从不同角度评估模型的预测效果。
- **遇到的问题与解决方法**：* 在数据爬取阶段，部分网站设置了反爬虫机制，导致数据获取失败。通过模拟浏览器请求头、设置合理的请求间隔、使用代理 IP 等方式解决了该问题；模型训练初期，出现了过拟合现象，预测结果在训练集上表现良好，但在验证集上误差较大。通过增加数据增强、调整正则化参数、早停策略等方法，有效缓解了过拟合问题；在模型训练过程中，由于硬件资源限制，训练速度较慢，通过使用 GPU 加速训练，并优化代码逻辑，减少不必要的计算开销，提高了训练效率。​

**2.1.3 成果与收获**：

- **项目最终成果展示**：通过可视化的波动曲线，直观呈现出预测的 6 月份猪肉价格走势，与实际价格趋势具备一定的拟合度，能够反映出价格的大致波动情况，为市场分析提供参考依据。
- **从项目中学习成果**：在数据处理方面，熟练掌握了使用 Python 结合 BeautifulSoup 进行网页数据爬取，对非结构化数据进行清洗、转换和整理，学会处理缺失值、异常值等问题；在模型构建上，深入理解了卷积层、LSTM 层、GRU 层和注意力层的原理与应用场景，能够根据时间序列数据特点合理设计模型架构；在训练调参过程中，积累了学习率调整、批次大小选择等优化技巧，明白了不同超参数对模型性能的影响；同时，强化了对 PyTorch 深度学习框架的使用，提升了代码编写和调试能力。
- **项目中学习的启示**：时间序列预测受多种复杂因素影响，单一模型很难做到完全精准预测，未来可尝试将多种模型融合，或者结合传统统计方法，提升预测的准确性；在数据收集阶段，除了价格数据，应进一步考虑宏观经济指标、养殖成本、政策法规等外部因素对猪肉价格的影响，拓展数据维度；在项目实践中，遇到问题时应注重团队协作和技术社区交流，借助集体智慧解决难题，同时要培养自己系统性分析问题和解决问题的能力，从数据、模型、算法等多方面寻找优化方向，以适应不断变化的实际应用场景 。

## 第三章    技术笔记汇总（选做）

| 问题描述        | 解决方案    |  参考资料  |
| --------   | ------   | ----  |
|      |  |       |
|      |  |       |
|      |  |       |

## 第四章    反思与展望

### 4.1 学习反思

- **本学期学习中最大的收获是什么？**：通过参与猪肉价格预测项目，实现了从理论知识到实践应用的跨越。在技术层面，熟练掌握了 Python 爬虫技术、PyTorch 深度学习框架的使用，能够独立完成数据处理、模型搭建与训练全流程操作；在思维层面，培养了数据驱动的问题解决思维，学会从海量数据中挖掘关键信息，并通过模型优化提升预测精度。此外，在项目实践过程中，深刻体会到团队协作的重要性，提升了沟通与项目管理能力。
- **哪些方面还需要加强？（如编程能力、数学基础、英文文献阅读等）**：* 编程能力上，虽然能够完成基础代码编写，但在复杂算法实现和代码优化方面仍有不足，面对大规模数据处理和高并发场景时，代码效率有待提高；数学基础方面，对深度学习算法背后的数学原理理解不够深入，如卷积层的数学推导、注意力机制的数学建模等，影响了对模型的进一步优化；英文文献阅读能力较弱，在查阅前沿学术论文时，存在理解障碍，难以快速掌握国际最新研究动态。​

4.2 未来计划

### 2.1 未来计划

- **是否打算将所学知识应用于科研项目或毕业论文？**：计划将深度学习在时间序列预测领域的应用作为毕业论文方向，进一步优化猪肉价格预测模型，同时探索将其应用于其他农产品价格预测，如牛肉、羊肉等，为农业经济领域的研究提供新的方法和思路。在科研项目中，也希望能够运用数据挖掘与机器学习技术，解决实际科研问题，提升科研创新能力。
- **是否有继续深入学习的方向（如 NLP、CV、强化学习等）？**：计划深入学习强化学习领域知识，探索强化学习在动态决策场景中的应用，如智能交易策略制定、资源调度优化等。同时，关注自然语言处理（NLP）与计算机视觉（CV）的交叉领域，学习多模态数据处理技术，拓宽自己的技术视野。
- **是否计划参与相关竞赛（如 Kaggle、天池）？**：* 计划积极参与 Kaggle、天池等数据科学竞赛，通过实战检验所学知识，接触更多前沿问题和优秀解决方案，与全球优秀的数据科学家交流学习，提升自身的竞争力和项目经验。同时，在竞赛过程中锻炼自己的抗压能力和快速学习能力，为未来职业发展积累宝贵经验。​

## 第五章    附录
